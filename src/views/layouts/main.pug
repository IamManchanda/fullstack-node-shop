doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    link(rel='stylesheet', href='https://cdnjs.cloudflare.com/ajax/libs/foundation/6.5.3/css/foundation-prototype.min.css')
    link(rel="stylesheet", href="/assets/css/styles.css")
    title #{ documentTitle }
  body
    //- Header
    header
      .top-bar
        .top-bar-left
          ul.menu
            li.menu-text Harry's Shop
            li(class=(path === '/' ? 'is-active' : ''))
              a(href='/' class=(path === '/' ? 'radius' : '')) 
                | Home
            li(class=(path === '/admin/add-product' ? 'is-active' : ''))
              a(href='/admin/add-product' class=(path === '/admin/add-product' ? 'radius' : '')) 
                | Add Product
    //- Body
    .grid-container.fluid
      .grid-x.grid-padding-x.grid-padding-y
        .cell
          main
            block content
